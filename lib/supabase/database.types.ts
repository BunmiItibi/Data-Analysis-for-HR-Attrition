/**
 * Placeholder Supabase database types. Replace with the real generated
 * types once a Supabase project exists, e.g.:
 *
 *   npx supabase gen types typescript --project-id <ref> > lib/supabase/database.types.ts
 *
 * Left loose here so the app builds without a live project; the actual
 * shape enforced at runtime is the SQL in supabase/migrations/.
 */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
